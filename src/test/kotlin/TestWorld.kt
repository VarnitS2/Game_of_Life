import io.kotest.core.spec.style.StringSpec
import io.kotest.matchers.shouldBe

class TestWorld : StringSpec({
    // "text" {
    //     val world = World(400, 400)
    //     world.setup()
    //     val validNeighbors = listOf(Cell(10, 0), Cell(10, 10), Cell(0, 10))
    //     val funcNeighbors = world.getNeighborCells(Cell(0, 0))
    //
    //     funcNeighbors shouldBe validNeighbors
    // }
})